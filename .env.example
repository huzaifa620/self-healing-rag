# Generation + critique + embeddings
OPENAI_API_KEY=sk-...

# Judge only. Deliberately a different provider from the generator so the
# system cannot grade its own homework.
ANTHROPIC_API_KEY=sk-ant-...
